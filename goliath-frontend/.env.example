# Je copie ce fichier en ".env" avant de lancer mon frontend.

# L'adresse de mon API backend. En local, mon backend tourne sur le
# port 4000 par défaut (voir le README du backend).
VITE_API_URL=http://localhost:4000
